{
  "name": "joy-baumann-website",
  "private": true,
  "type": "module",
  "scripts": {
    "db:migrate": "node --env-file=.env.local scripts/migrate.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
